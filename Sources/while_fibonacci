using System; 
using static System.Console;

public class Program
{
  public static void Main()
  {
    // Fibonacci
    int elementos = 7, i = 0;
    int actual = 1, sigui = 1;
    while(i < elementos)
    {
      WriteLine(actual);
      sigui = actual + (actual = sigui);
      i++;
    }
  }
}
